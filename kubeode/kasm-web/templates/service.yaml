apiVersion: v1
kind: Service
metadata:
  name: new-kasm-web
spec:
  type: NodePort
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}  # NodePort 设置
  selector:
    app: kasm-web
